package com.SpikeJava;

public class ArraysMain {
    public static void main(String[] args) {
        //int[] notas = new int[10];
        // notas[0] = 8;
        //notas[1] = 10;
        //System.out.println(notas[0]); // 8

        //int[] notas = {8,10,5,9,8,7,5,6};

        /*for (int i = 0; i < notas.length; i++) {
            System.out.println(notas[i]);
        }

        for(int item:notas) {
            System.out.println(item);
        }*/

        /*notas.binarySearch(notas, 9); // retorna el indice o elemento que está la posicion 9
        notas.sort(notas); // ordena los elementos
        notas.equals(array1,array2); // verifica si los elementos son iguales*/


    }
}
